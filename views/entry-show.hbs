<a href="/">Back to Home</a>
<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <h1>{{entries.title}}</h1>
        <h3>{{entries.content}}</h3>
        <p>{{entries.createdAt}}</p>
        <p><a href="/entries/{{entries._id}}/edit">Edit</a></p>
        <!-- Delete button -->
        <p><form method="POST" action="/entries/{{entries._id}}?_method=DELETE">
        <button type="submit">Delete</button>
        </form></p>
    </div>
</div>